import { DefaultTheme } from 'react-native-paper';

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#6200ee',        // Primary color for your app
    accent: '#03dac4',         // Accent color for your app
    background: '#ffffff',
    surface: '#ffffff',
    text: '#000000',
    // Add more colors if needed
  },
};
